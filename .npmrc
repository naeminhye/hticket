# Hoist Prisma packages to root node_modules so the generated
# .prisma/client can be found by @prisma/client at runtime.
# Required when using pnpm with Prisma.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=*@prisma*
